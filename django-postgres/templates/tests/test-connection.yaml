apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "django-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "django-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "django-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
